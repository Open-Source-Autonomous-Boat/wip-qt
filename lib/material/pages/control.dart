import 'package:flutter/material.dart';

class AndroidController extends StatelessWidget {
  const AndroidController({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text("WIP")),
    );
  }
}
